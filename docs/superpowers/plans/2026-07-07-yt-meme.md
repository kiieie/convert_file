# YouTube 밈 생성기 (yt-meme) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** YouTube URL로 구간을 골라 (B) 공유 클립 링크와 (A) 업로드 파일 기반 텍스트 GIF를 만드는 신규 도구.

**Architecture:** 정적 사이트, 빌드 없음, 바닐라 JS IIFE. 신규 페이지 2개(`yt-meme.html`, `clip-view.html`)와 공용 모듈 2개(`js/core/yt-utils.js`, `js/core/meme-text.js`). 구간 선택은 YouTube IFrame Player API(재생 제어만 가능), GIF 픽셀은 사용자 업로드 파일에서 video-gif의 프레임 캡처→gifshot 파이프라인 계승.

**Tech Stack:** 바닐라 JS, YouTube IFrame Player API, gifshot(`js/vendors/gifshot.min.js`), Playwright e2e.

**Spec:** `docs/superpowers/specs/2026-07-07-yt-meme-design.md`

## Global Constraints

- 모든 JS는 IIFE + 전역 네임스페이스 객체 패턴 (`window.YTUtils`처럼). 모듈 시스템/빌드 도구 없음.
- **YouTube 플레이어 화면 위에 어떤 요소도 겹치지 않는다** (개발자 정책). 텍스트 바는 플레이어 위/아래 별도 블록.
- 서버 저장 없음. 클립 데이터는 전부 URL 쿼리 파라미터.
- UI는 파일럿 패턴 준수: `.tool-flow` 단일 컬럼, `.advanced-settings`(details) 접힘, `.btn-convert-hero` hero 버튼. CSS는 기존 클래스 재사용, 페이지 전용 스타일은 해당 html `<style>` 블록에.
- GIF 구간 상한 15초 (video-gif과 동일, 메모리 보호).
- 유닛 테스트는 `test/tests.js`(브라우저)와 `test/run-tests.js`(Node 미러) **양쪽에 동일 케이스 추가** — 기존 관례.
- 유닛 테스트 실행: `cd test && node run-tests.js`. e2e: 레포 루트에서 `python3 -m http.server 8080` 띄운 뒤 `cd test && npx playwright test`.
- 커밋 메시지는 Conventional Commits, 본문 마지막에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## 파일 구조

| 파일 | 책임 |
|---|---|
| `js/core/yt-utils.js` (신규) | URL 파싱/클립 파라미터 검증·생성 — 순수 함수, 유닛 테스트 대상 |
| `js/core/meme-text.js` (신규) | 밈 텍스트 줄바꿈·렌더 — meme-generator와 yt-meme 공유 |
| `js/tools/meme-generator.js` (수정) | 내부 `drawWrappedText`를 MemeText 사용으로 교체 |
| `tools/yt-meme.html` (신규) | 메인 도구 페이지 |
| `js/tools/yt-meme.js` (신규) | 플레이어/구간/텍스트/공유/GIF 로직 |
| `tools/clip-view.html` + `js/tools/clip-view.js` (신규) | 공유 클립 뷰어 (언어 중립, 로케일 복사본 없음, noindex) |
| `js/core/sidebar.js` (수정) | 메뉴 등록 (4개 언어 라벨) |
| `ko|es|zh/tools/yt-meme.html` (신규) | 로케일 페이지 (동일 DOM id, 라벨만 번역) |
| `sitemap.xml` (수정) | yt-meme hreflang 세트 (clip-view는 noindex라 제외) |

---

### Task 1: YTUtils — URL 파서 + 클립 파라미터 (유닛 테스트 포함)

**Files:**
- Create: `js/core/yt-utils.js`
- Modify: `test/tests.js` (testCases 배열 끝에 추가), `test/run-tests.js` (loadScript + addTestCase), `test/index.html` (script 태그)

**Interfaces (Produces):**
- `YTUtils.parseYouTubeUrl(input: string) → { id: string, start: number|null } | null`
- `YTUtils.parseTimeParam(v: string|null) → number|null` — `"90"`, `"1m30s"`, `"1h2m3s"` 지원
- `YTUtils.validateClipParams(sp: URLSearchParams) → { v, s, e, t, b } | null`
- `YTUtils.buildClipUrl(base: string, p: {v,s,e,t?,b?}) → string`

- [ ] **Step 1: 실패하는 유닛 테스트 작성**

`test/run-tests.js`의 addTestCase 블록 끝(기존 케이스들 뒤)에 추가:

```js
addTestCase("YTUtils.parseYouTubeUrl() - YouTube URL 형식별 파싱 검증", () => {
    const r1 = YTUtils.parseYouTubeUrl("https://www.youtube.com/watch?v=dQw4w9WgXcQ");
    const r2 = YTUtils.parseYouTubeUrl("https://youtu.be/dQw4w9WgXcQ?t=42");
    const r3 = YTUtils.parseYouTubeUrl("https://www.youtube.com/shorts/dQw4w9WgXcQ");
    const r4 = YTUtils.parseYouTubeUrl("https://www.youtube.com/embed/dQw4w9WgXcQ");
    assert(r1 && r1.id === "dQw4w9WgXcQ", `watch 파싱 실패: ${JSON.stringify(r1)}`);
    assert(r2 && r2.id === "dQw4w9WgXcQ" && r2.start === 42, `youtu.be+t 파싱 실패: ${JSON.stringify(r2)}`);
    assert(r3 && r3.id === "dQw4w9WgXcQ", `shorts 파싱 실패: ${JSON.stringify(r3)}`);
    assert(r4 && r4.id === "dQw4w9WgXcQ", `embed 파싱 실패: ${JSON.stringify(r4)}`);
    assert(YTUtils.parseYouTubeUrl("https://example.com/watch?v=dQw4w9WgXcQ") === null, "타 도메인 거부 실패");
    assert(YTUtils.parseYouTubeUrl("not a url") === null, "불량 문자열 거부 실패");
    assert(YTUtils.parseYouTubeUrl("https://www.youtube.com/watch?v=short") === null, "11자 미만 ID 거부 실패");
});

addTestCase("YTUtils.parseTimeParam() - 타임스탬프 표기 변환 검증", () => {
    assert(YTUtils.parseTimeParam("90") === 90, "정수초 파싱 실패");
    assert(YTUtils.parseTimeParam("1m30s") === 90, "1m30s 파싱 실패");
    assert(YTUtils.parseTimeParam("1h2m3s") === 3723, "1h2m3s 파싱 실패");
    assert(YTUtils.parseTimeParam(null) === null, "null 처리 실패");
    assert(YTUtils.parseTimeParam("abc") === null, "불량 값 거부 실패");
});

addTestCase("YTUtils 클립 파라미터 - 생성/검증 라운드트립 (한글 텍스트)", () => {
    const url = YTUtils.buildClipUrl("https://2convert.org/tools/clip-view.html",
        { v: "dQw4w9WgXcQ", s: 10.5, e: 15, t: "상단 텍스트", b: "BOTTOM 😂" });
    const sp = new URLSearchParams(url.split("?")[1]);
    const p = YTUtils.validateClipParams(sp);
    assert(p !== null, "라운드트립 검증 실패(null)");
    assert(p.v === "dQw4w9WgXcQ" && p.s === 10.5 && p.e === 15, `v/s/e 불일치: ${JSON.stringify(p)}`);
    assert(p.t === "상단 텍스트" && p.b === "BOTTOM 😂", `텍스트 인코딩 라운드트립 실패: ${JSON.stringify(p)}`);
    assert(YTUtils.validateClipParams(new URLSearchParams("v=bad&s=0&e=5")) === null, "불량 ID 거부 실패");
    assert(YTUtils.validateClipParams(new URLSearchParams("v=dQw4w9WgXcQ&s=10&e=5")) === null, "e<=s 거부 실패");
});
```

같은 파일 상단 loadScript 목록(113행 부근)에 추가:

```js
loadScript('../js/core/yt-utils.js');
```

Node 환경에 `URL`/`URLSearchParams`는 내장이므로 추가 모킹 불필요.

`test/tests.js`의 `testCases` 배열 끝에 위와 동일한 3개 케이스를 `{ name: "...", run: function() {...} }` 형태로 추가 (코드 동일).

`test/index.html`의 `/js/core/file-utils.js` script 태그 다음 줄에 추가:

```html
    <script src="/js/core/yt-utils.js?v=1"></script>
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd test && node run-tests.js`
Expected: 신규 3개 케이스 FAIL (`YTUtils is not defined`), 기존 케이스 전부 PASS

- [ ] **Step 3: 구현**

`js/core/yt-utils.js` 생성:

```js
/**
 * ==========================================================================
 * ConvertFile - YouTube URL & Clip Param Utilities (yt-utils.js)
 * ==========================================================================
 */

(function (global) {
    'use strict';

    const ID_RE = /^[A-Za-z0-9_-]{11}$/;

    function parseTimeParam(v) {
        if (!v || typeof v !== 'string') return null;
        if (/^\d+$/.test(v)) return parseInt(v, 10);
        const m = v.match(/^(?:(\d+)h)?(?:(\d+)m)?(?:(\d+)s?)?$/);
        if (!m || (!m[1] && !m[2] && !m[3])) return null;
        return (parseInt(m[1] || 0, 10) * 3600) + (parseInt(m[2] || 0, 10) * 60) + parseInt(m[3] || 0, 10);
    }

    function parseYouTubeUrl(input) {
        if (!input || typeof input !== 'string') return null;
        let url;
        try {
            url = new URL(input.trim());
        } catch (e) {
            return null;
        }
        const host = url.hostname.replace(/^(www\.|m\.)/, '');
        let id = null;
        if (host === 'youtu.be') {
            id = url.pathname.slice(1).split('/')[0];
        } else if (host === 'youtube.com' || host === 'music.youtube.com' || host === 'youtube-nocookie.com') {
            if (url.pathname === '/watch') {
                id = url.searchParams.get('v');
            } else {
                const m = url.pathname.match(/^\/(shorts|embed|live)\/([A-Za-z0-9_-]{11})/);
                if (m) id = m[2];
            }
        }
        if (!id || !ID_RE.test(id)) return null;
        return { id: id, start: parseTimeParam(url.searchParams.get('t') || url.searchParams.get('start')) };
    }

    function validateClipParams(sp) {
        const v = sp.get('v');
        const s = parseFloat(sp.get('s'));
        const e = parseFloat(sp.get('e'));
        if (!v || !ID_RE.test(v)) return null;
        if (!isFinite(s) || !isFinite(e) || s < 0 || e <= s) return null;
        return { v: v, s: s, e: e, t: sp.get('t') || '', b: sp.get('b') || '' };
    }

    function buildClipUrl(base, p) {
        const q = new URLSearchParams();
        q.set('v', p.v);
        q.set('s', String(p.s));
        q.set('e', String(p.e));
        if (p.t) q.set('t', p.t);
        if (p.b) q.set('b', p.b);
        return base + '?' + q.toString();
    }

    global.YTUtils = {
        parseYouTubeUrl: parseYouTubeUrl,
        parseTimeParam: parseTimeParam,
        validateClipParams: validateClipParams,
        buildClipUrl: buildClipUrl
    };
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd test && node run-tests.js`
Expected: 전체 PASS (기존 + 신규 3)

- [ ] **Step 5: Commit**

```bash
git add js/core/yt-utils.js test/tests.js test/run-tests.js test/index.html
git commit -m "feat: add YTUtils for YouTube URL parsing and clip params"
```

---

### Task 2: MemeText 공용 모듈 추출

**Files:**
- Create: `js/core/meme-text.js`
- Modify: `js/tools/meme-generator.js:94-126` (drawWrappedText 제거·호출 교체)
- Modify: `tools/meme-generator.html`, `ko/tools/meme-generator.html`, `es/tools/meme-generator.html`, `zh/tools/meme-generator.html` (script 태그 추가)
- Modify: `test/tests.js`, `test/run-tests.js`, `test/index.html` (유닛 테스트)

**Interfaces:**
- Produces: `MemeText.wrapLines(measure: (s)=>number, text, maxWidth) → string[]`
- Produces: `MemeText.drawWrappedText(ctx, text, x, y, maxWidth, lineHeight, isTop)` — fill+stroke 렌더. 기존 meme-generator 내부 함수와 동일 동작, `ctx` 인자만 추가.

- [ ] **Step 1: 실패하는 유닛 테스트 작성**

`test/run-tests.js`에 추가 (loadScript 목록에 `loadScript('../js/core/meme-text.js');`도 추가):

```js
addTestCase("MemeText.wrapLines() - 밈 텍스트 줄바꿈 계산 검증", () => {
    const measure = (s) => s.length * 10; // 글자당 10px 모의 측정
    const r1 = MemeText.wrapLines(measure, "AAA BBB CCC", 80);
    const r2 = MemeText.wrapLines(measure, "SHORT", 200);
    const r3 = MemeText.wrapLines(measure, "", 100);
    assert(r1.length === 2 && r1[0] === "AAA BBB" && r1[1] === "CCC", `줄바꿈 분할 오류: ${JSON.stringify(r1)}`);
    assert(r2.length === 1 && r2[0] === "SHORT", `단일 줄 오류: ${JSON.stringify(r2)}`);
    assert(r3.length === 1 && r3[0] === "", `빈 문자열 오류: ${JSON.stringify(r3)}`);
});
```

`test/tests.js`에 동일 케이스 추가, `test/index.html`에 `<script src="/js/core/meme-text.js?v=1"></script>` 추가 (yt-utils 태그 다음 줄).

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd test && node run-tests.js`
Expected: 신규 케이스 FAIL (`MemeText is not defined`)

- [ ] **Step 3: 구현**

`js/core/meme-text.js` 생성:

```js
/**
 * ==========================================================================
 * ConvertFile - Meme Text Rendering Helpers (meme-text.js)
 * ==========================================================================
 */

(function (global) {
    'use strict';

    function wrapLines(measure, text, maxWidth) {
        const words = String(text).split(' ');
        const lines = [];
        let current = words[0] || '';
        for (let i = 1; i < words.length; i++) {
            if (measure(current + ' ' + words[i]) < maxWidth) {
                current += ' ' + words[i];
            } else {
                lines.push(current);
                current = words[i];
            }
        }
        lines.push(current);
        return lines;
    }

    function drawWrappedText(ctx, text, x, y, maxWidth, lineHeight, isTop) {
        const lines = wrapLines(function (s) { return ctx.measureText(s).width; }, text, maxWidth);
        if (isTop) {
            lines.forEach(function (line, i) {
                ctx.fillText(line, x, y + (i * lineHeight));
                ctx.strokeText(line, x, y + (i * lineHeight));
            });
        } else {
            const totalH = (lines.length - 1) * lineHeight;
            lines.forEach(function (line, i) {
                ctx.fillText(line, x, y - totalH + (i * lineHeight));
                ctx.strokeText(line, x, y - totalH + (i * lineHeight));
            });
        }
    }

    global.MemeText = { wrapLines: wrapLines, drawWrappedText: drawWrappedText };
})(typeof window !== 'undefined' ? window : globalThis);
```

`js/tools/meme-generator.js` 수정: 내부 `drawWrappedText` 함수(94~126행) 전체 삭제, `drawMeme()` 안의 두 호출을 교체:

```js
// 변경 전
drawWrappedText(topVal, canvas.width / 2, padding, maxWidth, lineHeight, true);
drawWrappedText(bottomVal, canvas.width / 2, canvas.height - padding, maxWidth, lineHeight, false);
// 변경 후
MemeText.drawWrappedText(ctx, topVal, canvas.width / 2, padding, maxWidth, lineHeight, true);
MemeText.drawWrappedText(ctx, bottomVal, canvas.width / 2, canvas.height - padding, maxWidth, lineHeight, false);
```

4개 로케일 `tools/meme-generator.html`: `/js/core/ui-components.js` script 태그 바로 다음 줄에 추가 (defer 순서상 tools 스크립트보다 앞이기만 하면 됨):

```html
    <script src="/js/core/meme-text.js?v=1" defer></script>
```

- [ ] **Step 4: 테스트 통과 + 기존 도구 회귀 확인**

Run: `cd test && node run-tests.js` → 전체 PASS
Run: 레포 루트에서 `python3 -m http.server 8080` 후 브라우저(또는 Playwright 스크린샷)로 `http://127.0.0.1:8080/tools/meme-generator.html` 열어 이미지 업로드→텍스트 입력→캔버스에 텍스트 렌더 확인 (콘솔 에러 0)

- [ ] **Step 5: Commit**

```bash
git add js/core/meme-text.js js/tools/meme-generator.js tools/meme-generator.html ko/tools/meme-generator.html es/tools/meme-generator.html zh/tools/meme-generator.html test/tests.js test/run-tests.js test/index.html
git commit -m "refactor: extract meme text rendering into shared MemeText module"
```

---

### Task 3: yt-meme.html 페이지 + 사이드바 등록

**Files:**
- Create: `tools/yt-meme.html`
- Modify: `js/core/sidebar.js` (4개 언어 라벨 + 메뉴 항목)

**Interfaces:**
- Produces: Task 4·5·7의 JS가 참조할 DOM id들 — `yt-url`, `btn-load-video`, `url-error`, `meme-frame`, `bar-top`, `bar-bottom`, `yt-player`, `segment-card`, `btn-set-start`, `btn-set-end`, `seg-start`, `seg-end`, `btn-preview-loop`, `text-card`, `top-text`, `bottom-text`, `uppercase-text`, `btn-copy-link`, `btn-copy-embed`, `clip-url-output`, `share-card`, `gif-file-input`, `gif-source-video`, `gif-start`, `gif-end`, `gif-width`, `gif-fps`, `gif-font-size`, `gif-text-color`, `gif-stroke-color`, `gif-stroke-width`, `btn-make-gif`, `gif-preview`, `gif-clamp-warning`

- [ ] **Step 1: 페이지 생성**

`tools/compress.html`의 1~220행 구조(애드센스/gtag/CSS 링크/사이드바 aside/`<main class="modern-content">` 래퍼)를 복사하되 다음 값 교체:

- `<title>YouTube Clip Meme Maker - ConvertFile</title>`
- description: `Pick a segment from a YouTube video, add meme text, share a clip link or export a real GIF from your own video file. 100% client-side.`
- keywords: `youtube meme, youtube clip, video meme maker, gif meme`
- canonical/og:url/twitter:url: `https://2convert.org/tools/yt-meme.html`
- og:image/twitter:image: `https://2convert.org/assets/icon-512.png`
- ld+json SoftwareApplication: name `YouTube Clip Meme Maker`, url 위와 동일, featureList `YouTube segment selection,Shareable clip link,Meme text bars,GIF export`

`<main>` 내부 도구 영역 마크업:

```html
<h1>😂 YouTube Clip Meme Maker</h1>
<p class="tool-subtitle">Pick a segment, add meme text, share it — or turn it into a real GIF.</p>

<div class="tool-flow">

    <!-- Step 1: URL -->
    <div class="modern-card" id="url-card">
        <label for="yt-url"><strong>YouTube URL</strong></label>
        <div class="yt-url-row">
            <input type="url" id="yt-url" placeholder="https://www.youtube.com/watch?v=..." autocomplete="off">
            <button class="modern-btn" id="btn-load-video">Load</button>
        </div>
        <p class="input-error" id="url-error" hidden></p>
    </div>

    <!-- Step 2: player + meme bars (bars sit OUTSIDE the player, never over it) -->
    <div id="meme-frame" hidden>
        <div class="meme-bar" id="bar-top" hidden></div>
        <div class="yt-player-box"><div id="yt-player"></div></div>
        <div class="meme-bar" id="bar-bottom" hidden></div>
    </div>

    <!-- Step 3: segment -->
    <div class="modern-card" id="segment-card" hidden>
        <strong>Clip segment</strong>
        <div class="seg-row">
            <button class="modern-btn" id="btn-set-start">⏱ Set start here</button>
            <input type="number" id="seg-start" min="0" step="0.1" value="0"> s
            <button class="modern-btn" id="btn-set-end">⏱ Set end here</button>
            <input type="number" id="seg-end" min="0.5" step="0.1" value="5"> s
            <button class="modern-btn" id="btn-preview-loop">🔁 Preview loop</button>
        </div>
    </div>

    <!-- Step 4: text -->
    <div class="modern-card" id="text-card" hidden>
        <strong>Meme text</strong>
        <input type="text" id="top-text" placeholder="Top text" maxlength="200">
        <input type="text" id="bottom-text" placeholder="Bottom text" maxlength="200">
        <label><input type="checkbox" id="uppercase-text" checked> UPPERCASE</label>
    </div>

    <!-- B output: share -->
    <div id="share-card" hidden>
        <button class="btn-convert-hero" id="btn-copy-link" disabled>🔗 Copy Clip Link</button>
        <div class="share-row">
            <input type="text" id="clip-url-output" readonly placeholder="Clip link appears here">
            <button class="modern-btn" id="btn-copy-embed">Copy embed code</button>
        </div>
    </div>

    <!-- A output: real GIF (collapsed) -->
    <details class="advanced-settings" id="gif-section">
        <summary>🎞️ Need a real GIF file? Upload the video file</summary>
        <div class="advanced-settings__body">
            <p class="gif-note">YouTube pixels can't be read by the browser. Upload a video file containing this
               segment (your own recording or original file) and we'll render the GIF locally — nothing is uploaded to a server.</p>
            <input type="file" id="gif-file-input" accept="video/*">
            <video id="gif-source-video" class="modern-video-player" controls hidden></video>
            <p class="input-error" id="gif-clamp-warning" hidden></p>
            <div class="seg-row">
                <label>Start <input type="number" id="gif-start" min="0" step="0.1" value="0"> s</label>
                <label>End <input type="number" id="gif-end" min="0.5" step="0.1" value="5"> s</label>
                <label>Width <input type="number" id="gif-width" min="80" max="720" value="320"> px</label>
                <label>FPS <select id="gif-fps"><option>5</option><option selected>10</option><option>15</option></select></label>
            </div>
            <div class="seg-row">
                <label>Font size <input type="number" id="gif-font-size" min="10" max="80" value="28"> px</label>
                <label>Text <input type="color" id="gif-text-color" value="#FFFFFF"></label>
                <label>Stroke <input type="color" id="gif-stroke-color" value="#000000"></label>
                <label>Stroke width <input type="number" id="gif-stroke-width" min="0" max="10" value="2"> px</label>
            </div>
            <button class="btn-convert-hero" id="btn-make-gif" disabled>🎞️ Make GIF</button>
            <img id="gif-preview" alt="GIF preview" hidden>
        </div>
    </details>
</div>
```

페이지 `<style>` 블록 (head, video-gif.html의 `.modern-ad-zone` 스타일 위치와 동일하게):

```css
.yt-player-box { position: relative; width: 100%; aspect-ratio: 16 / 9; background: #000; }
.yt-player-box iframe, .yt-player-box > div { position: absolute; inset: 0; width: 100%; height: 100%; }
.meme-bar {
    background: #FFFFFF; color: #000000; text-align: center;
    font-family: Impact, "Arial Black", sans-serif; font-size: clamp(18px, 4vw, 32px);
    padding: 10px 14px; line-height: 1.15; word-break: break-word;
    border: 1px solid var(--slate-200);
}
#meme-frame { border-radius: var(--radius-md); overflow: hidden; }
.yt-url-row, .seg-row, .share-row { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; }
.yt-url-row input[type="url"], .share-row input { flex: 1; min-width: 200px; }
.input-error { color: var(--error); font-size: 13px; }
.gif-note { font-size: 13px; color: var(--slate-500); }
#gif-preview { max-width: 100%; border-radius: var(--radius-sm); }
```

하단 script 목록 (compress.html 407~412행 패턴):

```html
    <script src="/js/vendors/gifshot.min.js?v=4" defer></script>
    <script src="/js/core/file-utils.js?v=3" defer></script>
    <script src="/js/core/ui-components.js?v=3" defer></script>
    <script src="/js/core/sidebar.js?v=4" defer></script>
    <script src="/js/core/yt-utils.js?v=1" defer></script>
    <script src="/js/core/meme-text.js?v=1" defer></script>
    <script src="/js/tools/yt-meme.js?v=1" defer></script>
```

- [ ] **Step 2: 사이드바 등록**

`js/core/sidebar.js` — 4개 언어 라벨 객체(en 30행/ko 65행/es 100행/zh 135행 부근)의 `memeGen` 라인 다음에 각각 추가:

```js
ytMeme:      "YouTube Meme",        // en
ytMeme:      "유튜브 밈",            // ko
ytMeme:      "Meme de YouTube",     // es
ytMeme:      "YouTube表情包",        // zh
```

메뉴 HTML(177행 부근, `menu-meme-gen` 라인 다음)에 추가:

```html
<a href="${langPrefix}/tools/yt-meme.html"            class="modern-sidebar__item" id="menu-yt-meme">🎬 ${t.ytMeme}</a>
```

- [ ] **Step 3: 렌더 확인**

Run: 레포 루트 `python3 -m http.server 8080` → `http://127.0.0.1:8080/tools/yt-meme.html`
Expected: 페이지 로드, 사이드바에 "YouTube Meme" 항목, URL 입력창 표시, 콘솔 에러는 `yt-meme.js` 404 하나만 (다음 Task에서 생성)

- [ ] **Step 4: Commit**

```bash
git add tools/yt-meme.html js/core/sidebar.js
git commit -m "feat: add yt-meme tool page and sidebar entry"
```

---

### Task 4: yt-meme.js — 플레이어 로드 + 구간 선택

**Files:**
- Create: `js/tools/yt-meme.js`

**Interfaces:**
- Consumes: `YTUtils.parseYouTubeUrl` (Task 1), Task 3의 DOM id들
- Produces: IIFE 내부 상태 `player`, `videoId`, `getSegment() → {s, e}` — Task 5/7 스텝이 같은 파일에 이어 붙임

- [ ] **Step 1: 구현**

`js/tools/yt-meme.js` 생성:

```js
/**
 * ==========================================================================
 * ConvertFile - YouTube Clip Meme Maker (yt-meme.js)
 * ==========================================================================
 * 구간 선택은 YouTube IFrame Player API(재생 제어 전용). 플레이어 픽셀은 읽지
 * 않으며, GIF 픽셀은 사용자가 업로드한 파일에서만 추출한다.
 */

(function () {
    'use strict';

    // --- DOM refs ---
    const urlInput = document.getElementById('yt-url');
    const btnLoad = document.getElementById('btn-load-video');
    const urlError = document.getElementById('url-error');
    const memeFrame = document.getElementById('meme-frame');
    const segmentCard = document.getElementById('segment-card');
    const textCard = document.getElementById('text-card');
    const shareCard = document.getElementById('share-card');
    const btnSetStart = document.getElementById('btn-set-start');
    const btnSetEnd = document.getElementById('btn-set-end');
    const segStartInput = document.getElementById('seg-start');
    const segEndInput = document.getElementById('seg-end');
    const btnPreviewLoop = document.getElementById('btn-preview-loop');

    // --- state ---
    let player = null;
    let videoId = null;
    let apiRequested = false;
    let previewTimer = null;

    function getSegment() {
        return { s: parseFloat(segStartInput.value) || 0, e: parseFloat(segEndInput.value) || 0 };
    }

    function segmentValid() {
        const seg = getSegment();
        return seg.s >= 0 && seg.e - seg.s >= 0.5;
    }

    function loadIframeApi() {
        return new Promise(function (resolve) {
            if (window.YT && window.YT.Player) { resolve(); return; }
            const prev = window.onYouTubeIframeAPIReady;
            window.onYouTubeIframeAPIReady = function () {
                if (prev) prev();
                resolve();
            };
            if (!apiRequested) {
                apiRequested = true;
                const s = document.createElement('script');
                s.src = 'https://www.youtube.com/iframe_api';
                document.head.appendChild(s);
            }
        });
    }

    function showUrlError(msg) {
        urlError.textContent = msg;
        urlError.hidden = false;
    }

    function onPlayerError(e) {
        let msg = 'This video cannot be played.';
        if (e.data === 101 || e.data === 150) msg = 'The owner of this video has disabled embedding. Try another video.';
        if (e.data === 100) msg = 'Video not found or private.';
        if (e.data === 2) msg = 'Invalid video id.';
        showUrlError(msg);
    }

    btnLoad.addEventListener('click', function () {
        const parsed = YTUtils.parseYouTubeUrl(urlInput.value);
        if (!parsed) {
            showUrlError('Invalid YouTube URL. Supported: youtube.com/watch, youtu.be, shorts, embed links.');
            return;
        }
        urlError.hidden = true;
        videoId = parsed.id;
        if (parsed.start !== null) {
            segStartInput.value = parsed.start;
            segEndInput.value = parsed.start + 5;
        }
        loadIframeApi().then(function () {
            if (player) {
                player.loadVideoById(videoId);
            } else {
                player = new YT.Player('yt-player', {
                    videoId: videoId,
                    playerVars: { rel: 0 },
                    events: { onError: onPlayerError }
                });
            }
            memeFrame.hidden = false;
            segmentCard.hidden = false;
            textCard.hidden = false;
            shareCard.hidden = false;
            updateShareState(); // Task 5에서 정의
        });
    });

    urlInput.addEventListener('keydown', function (e) {
        if (e.key === 'Enter') btnLoad.click();
    });

    // --- segment selection ---
    btnSetStart.addEventListener('click', function () {
        if (!player || !player.getCurrentTime) return;
        segStartInput.value = (Math.round(player.getCurrentTime() * 10) / 10).toFixed(1);
        if (parseFloat(segEndInput.value) <= parseFloat(segStartInput.value)) {
            segEndInput.value = (parseFloat(segStartInput.value) + 5).toFixed(1);
        }
        updateShareState();
    });

    btnSetEnd.addEventListener('click', function () {
        if (!player || !player.getCurrentTime) return;
        segEndInput.value = (Math.round(player.getCurrentTime() * 10) / 10).toFixed(1);
        updateShareState();
    });

    segStartInput.addEventListener('input', updateShareState);
    segEndInput.addEventListener('input', updateShareState);

    function stopPreviewLoop() {
        if (previewTimer) {
            clearInterval(previewTimer);
            previewTimer = null;
            btnPreviewLoop.textContent = '🔁 Preview loop';
            if (player && player.pauseVideo) player.pauseVideo();
        }
    }

    btnPreviewLoop.addEventListener('click', function () {
        if (!player || !segmentValid()) return;
        if (previewTimer) { stopPreviewLoop(); return; }
        const seg = getSegment();
        player.seekTo(seg.s, true);
        player.playVideo();
        btnPreviewLoop.textContent = '⏹ Stop preview';
        previewTimer = setInterval(function () {
            const cur = getSegment(); // 입력 변경 실시간 반영
            if (player.getCurrentTime() >= cur.e) player.seekTo(cur.s, true);
        }, 200);
    });

    // Task 5에서 실제 구현으로 교체되기 전 임시 no-op
    function updateShareState() {}
})();
```

- [ ] **Step 2: 브라우저 검증**

Run: `python3 -m http.server 8080` → `http://127.0.0.1:8080/tools/yt-meme.html`
검증: 유효 URL(`https://www.youtube.com/watch?v=jNQXAC9IVRw`) 입력→Load→플레이어 표시·재생, [Set start/end]로 입력값 갱신, Preview loop 구간 반복, 불량 URL 인라인 에러. 콘솔 에러 0.

- [ ] **Step 3: Commit**

```bash
git add js/tools/yt-meme.js
git commit -m "feat: yt-meme player load and segment selection via IFrame API"
```

---

### Task 5: yt-meme.js — 텍스트 바 + 공유 링크/embed

**Files:**
- Modify: `js/tools/yt-meme.js` (Task 4 파일에 이어서)

**Interfaces:**
- Consumes: `YTUtils.buildClipUrl` (Task 1), Task 4의 `videoId`/`getSegment`/`segmentValid`
- Produces: `displayText(raw) → string` (대문자 토글 적용) — Task 7이 GIF 텍스트에 재사용

- [ ] **Step 1: 구현**

Task 4 말미의 `function updateShareState() {}` no-op을 삭제하고 다음 블록으로 교체 (파일 내 `btnPreviewLoop` 리스너 뒤):

```js
    // --- meme text bars (player 밖 위/아래 — 플레이어 위 오버레이 금지 정책 준수) ---
    const topTextInput = document.getElementById('top-text');
    const bottomTextInput = document.getElementById('bottom-text');
    const uppercaseCheckbox = document.getElementById('uppercase-text');
    const barTop = document.getElementById('bar-top');
    const barBottom = document.getElementById('bar-bottom');
    const btnCopyLink = document.getElementById('btn-copy-link');
    const btnCopyEmbed = document.getElementById('btn-copy-embed');
    const clipUrlOutput = document.getElementById('clip-url-output');

    function displayText(raw) {
        const v = (raw || '').trim();
        return uppercaseCheckbox.checked ? v.toUpperCase() : v;
    }

    function updateBars() {
        const t = displayText(topTextInput.value);
        const b = displayText(bottomTextInput.value);
        barTop.textContent = t;
        barTop.hidden = t.length === 0;
        barBottom.textContent = b;
        barBottom.hidden = b.length === 0;
    }

    function updateShareState() {
        updateBars();
        btnCopyLink.disabled = !(videoId && segmentValid());
    }

    topTextInput.addEventListener('input', updateShareState);
    bottomTextInput.addEventListener('input', updateShareState);
    uppercaseCheckbox.addEventListener('change', updateShareState);

    function currentClipUrl() {
        const seg = getSegment();
        return YTUtils.buildClipUrl(location.origin + '/tools/clip-view.html', {
            v: videoId,
            s: Math.round(seg.s * 10) / 10,
            e: Math.round(seg.e * 10) / 10,
            t: displayText(topTextInput.value),
            b: displayText(bottomTextInput.value)
        });
    }

    function copyWithFeedback(btn, text, doneLabel) {
        const original = btn.textContent;
        navigator.clipboard.writeText(text).then(function () {
            btn.textContent = doneLabel;
            setTimeout(function () { btn.textContent = original; }, 1500);
        }).catch(function () {
            UIComponents.showErrorDialog('Clipboard Error', 'Copy failed. The link is shown in the box below — copy it manually.');
        });
    }

    btnCopyLink.addEventListener('click', function () {
        if (btnCopyLink.disabled) return;
        const url = currentClipUrl();
        clipUrlOutput.value = url;
        copyWithFeedback(btnCopyLink, url, '✅ Copied!');
    });

    btnCopyEmbed.addEventListener('click', function () {
        if (!videoId || !segmentValid()) return;
        const url = currentClipUrl();
        clipUrlOutput.value = url;
        const embed = '<iframe src="' + url + '" width="560" height="420" frameborder="0" allowfullscreen loading="lazy"></iframe>';
        copyWithFeedback(btnCopyEmbed, embed, '✅ Copied!');
    });
```

주의: Task 4에서 `updateShareState()`를 호출하는 지점(btnLoad 핸들러, seg 입력 리스너)은 함수 선언 호이스팅으로 그대로 동작 — `updateShareState`를 반드시 `function` 선언으로 유지.

- [ ] **Step 2: 브라우저 검증**

검증: 텍스트 입력 시 플레이어 위/아래 흰 바에 즉시 반영(빈 값이면 바 숨김), UPPERCASE 토글 동작, [Copy Clip Link] → 출력창에 `clip-view.html?v=..&s=..&e=..&t=..&b=..` URL·클립보드 복사, embed 코드 복사. 텍스트 바가 플레이어 영역과 겹치지 않음(스크린샷 확인).

- [ ] **Step 3: Commit**

```bash
git add js/tools/yt-meme.js
git commit -m "feat: yt-meme text bars and shareable clip link generation"
```

---

### Task 6: clip-view 뷰어 페이지

**Files:**
- Create: `tools/clip-view.html`
- Create: `js/tools/clip-view.js`

**Interfaces:**
- Consumes: `YTUtils.validateClipParams` (Task 1), Task 5가 생성하는 URL 스키마 `?v=&s=&e=&t=&b=`

- [ ] **Step 1: HTML 생성**

`tools/clip-view.html` — 사이드바 없는 단독 미니 페이지 (공유 수신자용). 전체 내용:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex">
    <title>Clip Meme - ConvertFile</title>
    <link rel="stylesheet" href="/css/modern-theme.css">
    <style>
        body { display: flex; flex-direction: column; align-items: center; justify-content: center;
               min-height: 100vh; margin: 0; background: var(--slate-50); padding: 16px; box-sizing: border-box; }
        .clip-frame { width: min(720px, 100%); border-radius: 12px; overflow: hidden;
                      box-shadow: 0 8px 30px rgba(0,0,0,0.12); }
        .yt-player-box { position: relative; width: 100%; aspect-ratio: 16 / 9; background: #000; }
        .yt-player-box iframe, .yt-player-box > div { position: absolute; inset: 0; width: 100%; height: 100%; }
        .meme-bar { background: #FFFFFF; color: #000000; text-align: center;
                    font-family: Impact, "Arial Black", sans-serif; font-size: clamp(18px, 4vw, 32px);
                    padding: 10px 14px; line-height: 1.15; word-break: break-word; }
        .clip-error { color: #dc2626; font-size: 15px; text-align: center; }
        .clip-footer { margin-top: 14px; font-size: 13px; }
        .clip-footer a { color: #2563eb; }
    </style>
</head>
<body>
    <div class="clip-frame" id="clip-frame" hidden>
        <div class="meme-bar" id="bar-top" hidden></div>
        <div class="yt-player-box"><div id="yt-player"></div></div>
        <div class="meme-bar" id="bar-bottom" hidden></div>
    </div>
    <p class="clip-error" id="clip-error" hidden>Invalid or broken clip link.</p>
    <p class="clip-footer">⚡ Make your own — <a href="/tools/yt-meme.html">ConvertFile YouTube Meme Maker</a></p>

    <script src="/js/core/yt-utils.js?v=1"></script>
    <script src="/js/tools/clip-view.js?v=1"></script>
</body>
</html>
```

- [ ] **Step 2: JS 생성**

`js/tools/clip-view.js` 전체:

```js
/**
 * ==========================================================================
 * ConvertFile - Shared Clip Viewer (clip-view.js)
 * ==========================================================================
 * 저장소 없음: 클립 데이터(영상 ID/구간/텍스트)는 전부 URL 파라미터.
 */

(function () {
    'use strict';

    const frame = document.getElementById('clip-frame');
    const errBox = document.getElementById('clip-error');
    const params = YTUtils.validateClipParams(new URLSearchParams(location.search));

    if (!params) {
        errBox.hidden = false;
        return;
    }

    const barTop = document.getElementById('bar-top');
    const barBottom = document.getElementById('bar-bottom');
    barTop.textContent = params.t;
    barTop.hidden = params.t.length === 0;
    barBottom.textContent = params.b;
    barBottom.hidden = params.b.length === 0;
    frame.hidden = false;

    let player = null;
    let loopTimer = null;

    window.onYouTubeIframeAPIReady = function () {
        player = new YT.Player('yt-player', {
            videoId: params.v,
            playerVars: { start: Math.floor(params.s), rel: 0 },
            events: {
                onStateChange: function (e) {
                    // end 파라미터는 seek 후 무시되므로 인터벌로 구간 루프 유지
                    if (e.data === YT.PlayerState.PLAYING && !loopTimer) {
                        loopTimer = setInterval(function () {
                            if (player.getCurrentTime() >= params.e) player.seekTo(params.s, true);
                        }, 200);
                    }
                },
                onError: function () {
                    frame.hidden = true;
                    errBox.textContent = 'This video is unavailable, private, or embedding is disabled.';
                    errBox.hidden = false;
                }
            }
        });
    };

    const s = document.createElement('script');
    s.src = 'https://www.youtube.com/iframe_api';
    document.head.appendChild(s);
})();
```

- [ ] **Step 3: 브라우저 검증**

- `http://127.0.0.1:8080/tools/clip-view.html?v=jNQXAC9IVRw&s=3&e=8&t=%EC%83%81%EB%8B%A8&b=BOTTOM` → 상/하단 바 텍스트 표시, 재생 시 3~8초 루프
- `...clip-view.html?v=bad` → "Invalid or broken clip link." 표시, 플레이어 없음
- yt-meme 페이지에서 [Copy Clip Link]로 만든 URL을 새 탭에 붙여넣어 라운드트립 확인 (한글 텍스트 포함)

- [ ] **Step 4: Commit**

```bash
git add tools/clip-view.html js/tools/clip-view.js
git commit -m "feat: add clip-view shared clip viewer page"
```

---

### Task 7: GIF 파이프라인 (업로드 파일 → 텍스트 GIF)

**Files:**
- Modify: `js/tools/yt-meme.js` (파일 끝, Task 5 블록 뒤에 추가)

**Interfaces:**
- Consumes: `MemeText.drawWrappedText` (Task 2), `displayText` (Task 5), `getSegment` (Task 4), `FileUtils.downloadFile`, `UIComponents.showProgressDialog/showErrorDialog`, `gifshot`
- Produces: 없음 (말단 기능)

- [ ] **Step 1: 구현**

`js/tools/yt-meme.js` IIFE 끝(`btnCopyEmbed` 리스너 뒤)에 추가:

```js
    // --- A output: real GIF from user-uploaded file (YouTube 서버 무접촉) ---
    const gifFileInput = document.getElementById('gif-file-input');
    const gifSourceVideo = document.getElementById('gif-source-video');
    const gifStartInput = document.getElementById('gif-start');
    const gifEndInput = document.getElementById('gif-end');
    const gifWidthInput = document.getElementById('gif-width');
    const gifFpsSelect = document.getElementById('gif-fps');
    const gifFontSize = document.getElementById('gif-font-size');
    const gifTextColor = document.getElementById('gif-text-color');
    const gifStrokeColor = document.getElementById('gif-stroke-color');
    const gifStrokeWidth = document.getElementById('gif-stroke-width');
    const btnMakeGif = document.getElementById('btn-make-gif');
    const gifPreview = document.getElementById('gif-preview');
    const gifClampWarning = document.getElementById('gif-clamp-warning');

    let gifFileName = null;

    gifFileInput.addEventListener('change', function () {
        const file = gifFileInput.files[0];
        if (!file) return;
        gifFileName = file.name;
        gifSourceVideo.src = URL.createObjectURL(file);
        gifSourceVideo.hidden = false;
        gifSourceVideo.onloadedmetadata = function () {
            const dur = gifSourceVideo.duration;
            // 1단계에서 고른 구간을 기본값으로, 파일 길이에 클램프
            const seg = getSegment();
            let s = Math.min(seg.s, Math.max(0, dur - 0.5));
            let e = Math.min(seg.e, dur);
            if (e - s < 0.5) { s = 0; e = Math.min(5, dur); }
            gifStartInput.value = s.toFixed(1);
            gifEndInput.value = e.toFixed(1);
            const clamped = seg.e > dur;
            gifClampWarning.hidden = !clamped;
            if (clamped) {
                gifClampWarning.textContent = 'Uploaded file is shorter than the selected segment — range clamped to ' + dur.toFixed(1) + 's.';
            }
            btnMakeGif.disabled = false;
        };
    });

    btnMakeGif.addEventListener('click', function () {
        if (typeof gifshot === 'undefined') {
            UIComponents.showErrorDialog('Library Error', 'gifshot library not loaded. Please refresh the page.');
            return;
        }
        const start = parseFloat(gifStartInput.value);
        const end = Math.min(parseFloat(gifEndInput.value), gifSourceVideo.duration || Infinity);
        const width = parseInt(gifWidthInput.value, 10) || 320;
        const fps = parseInt(gifFpsSelect.value, 10) || 10;

        if (!(start >= 0) || end <= start) {
            UIComponents.showErrorDialog('Invalid Range', 'End time must be greater than start time.');
            return;
        }
        if (end - start > 15) {
            UIComponents.showErrorDialog('Duration Limit', 'Max clip length is 15 seconds to protect browser memory.');
            return;
        }

        const frameInterval = 1 / fps;
        const totalFrames = Math.ceil((end - start) * fps);
        const captureTimes = [];
        for (let i = 0; i < totalFrames; i++) captureTimes.push(start + (i * frameInterval));

        const captureCanvas = document.createElement('canvas');
        const cctx = captureCanvas.getContext('2d');
        let ratio = gifSourceVideo.videoWidth / gifSourceVideo.videoHeight;
        if (!ratio || !isFinite(ratio) || ratio <= 0) ratio = 16 / 9;
        captureCanvas.width = width;
        captureCanvas.height = Math.round(width / ratio) || 180;

        const topVal = displayText(topTextInput.value);
        const bottomVal = displayText(bottomTextInput.value);
        const fontSize = parseInt(gifFontSize.value, 10) || 28;
        const padding = Math.max(6, Math.floor(captureCanvas.height * 0.03));
        const maxWidth = captureCanvas.width - 16;
        const lineHeight = fontSize + 4;

        function drawTexts() {
            cctx.font = 'bold ' + fontSize + 'px Impact, "Arial Black", sans-serif';
            cctx.textAlign = 'center';
            cctx.fillStyle = gifTextColor.value;
            cctx.strokeStyle = gifStrokeColor.value;
            cctx.lineWidth = parseInt(gifStrokeWidth.value, 10) || 0;
            cctx.lineJoin = 'round';
            if (topVal) {
                cctx.textBaseline = 'top';
                MemeText.drawWrappedText(cctx, topVal, captureCanvas.width / 2, padding, maxWidth, lineHeight, true);
            }
            if (bottomVal) {
                cctx.textBaseline = 'bottom';
                MemeText.drawWrappedText(cctx, bottomVal, captureCanvas.width / 2, captureCanvas.height - padding, maxWidth, lineHeight, false);
            }
        }

        const prog = UIComponents.showProgressDialog('Making GIF', 'Extracting frames... (0/' + totalFrames + ')');
        const framesData = [];
        let frameIdx = 0;

        function captureNextFrame() {
            if (frameIdx >= captureTimes.length) {
                prog.updateProgress(90, 'Building GIF animation...');
                gifshot.createGIF({
                    images: framesData,
                    gifWidth: captureCanvas.width,
                    gifHeight: captureCanvas.height,
                    interval: frameInterval,
                    numWorkers: 2
                }, function (obj) {
                    prog.updateProgress(100, 'Done!');
                    setTimeout(function () {
                        prog.close();
                        if (obj.error) {
                            UIComponents.showErrorDialog('GIF Error', 'GIF encoder failed.');
                            return;
                        }
                        gifPreview.src = obj.image;
                        gifPreview.hidden = false;
                        const base = (gifFileName || 'clip').replace(/\.[^.]+$/, '');
                        FileUtils.downloadFile(obj.image, base + '_meme.gif');
                    }, 400);
                });
                return;
            }
            gifSourceVideo.currentTime = captureTimes[frameIdx];
            gifSourceVideo.onseeked = function () {
                try {
                    cctx.drawImage(gifSourceVideo, 0, 0, captureCanvas.width, captureCanvas.height);
                    drawTexts();
                    framesData.push(captureCanvas.toDataURL('image/jpeg', 0.9));
                    frameIdx++;
                    prog.updateProgress(Math.round((frameIdx / totalFrames) * 85),
                        'Extracting frames... (' + frameIdx + '/' + totalFrames + ')');
                    captureNextFrame();
                } catch (err) {
                    prog.close();
                    let tip = (err.name === 'SecurityError') ? ' (CORS error: run via http server, not file://)' : '';
                    UIComponents.showErrorDialog('Capture Error', 'Frame capture failed: ' + err.message + tip);
                }
            };
        }

        gifSourceVideo.pause();
        captureNextFrame();
    });
```

- [ ] **Step 2: 브라우저 검증**

로컬 mp4 파일로 검증 (아무 짧은 mp4, 없으면 `test-results` 내 fixture 또는 QuickTime 녹화 5초):
- 업로드 → 구간 기본값이 1단계 선택 구간으로 프리필, 파일보다 길면 경고+클램프
- [Make GIF] → 진행 다이얼로그 → GIF 다운로드 + 미리보기 표시, 상/하단 텍스트가 GIF 프레임 안에 렌더됨
- 16초 구간 → Duration Limit 에러

- [ ] **Step 3: Commit**

```bash
git add js/tools/yt-meme.js
git commit -m "feat: yt-meme GIF export from uploaded video with text overlay"
```

---

### Task 8: e2e 테스트

**Files:**
- Modify: `test/e2e.test.js` (파일 끝에 test 블록 추가)

- [ ] **Step 1: 테스트 작성**

```js
// ==========================================================================
// yt-meme / clip-view E2E
// ==========================================================================
test('YT Meme - page loads with all controls and no JS errors', async ({ page }) => {
    const errors = [];
    page.on('pageerror', (e) => errors.push(e.message));
    await page.goto('http://127.0.0.1:8080/tools/yt-meme.html');
    await expect(page.locator('#yt-url')).toBeVisible();
    await expect(page.locator('#btn-load-video')).toBeVisible();
    await expect(page.locator('#gif-section')).toBeAttached();
    await expect(page.locator('#menu-yt-meme')).toBeVisible();
    expect(errors).toEqual([]);
});

test('YT Meme - invalid URL shows inline error', async ({ page }) => {
    await page.goto('http://127.0.0.1:8080/tools/yt-meme.html');
    await page.fill('#yt-url', 'https://example.com/watch?v=dQw4w9WgXcQ');
    await page.click('#btn-load-video');
    await expect(page.locator('#url-error')).toBeVisible();
});

test('Clip View - renders text bars from params (Korean roundtrip)', async ({ page }) => {
    await page.goto('http://127.0.0.1:8080/tools/clip-view.html?v=jNQXAC9IVRw&s=3&e=8&t=%EC%83%81%EB%8B%A8%20%ED%85%8D%EC%8A%A4%ED%8A%B8&b=BOTTOM');
    await expect(page.locator('#bar-top')).toHaveText('상단 텍스트');
    await expect(page.locator('#bar-bottom')).toHaveText('BOTTOM');
    await expect(page.locator('#clip-error')).toBeHidden();
});

test('Clip View - rejects malformed params', async ({ page }) => {
    await page.goto('http://127.0.0.1:8080/tools/clip-view.html?v=bad&s=10&e=5');
    await expect(page.locator('#clip-error')).toBeVisible();
    await expect(page.locator('#clip-frame')).toBeHidden();
});
```

- [ ] **Step 2: 실행**

Run: 레포 루트 `python3 -m http.server 8080` (백그라운드) → `cd test && npx playwright test`
Expected: 신규 4개 포함 전체 PASS. (기존 Core Engine e2e도 함께 돌며 Task 1~2 유닛 테스트가 브라우저에서도 PASS임을 확인)

- [ ] **Step 3: Commit**

```bash
git add test/e2e.test.js
git commit -m "test: add yt-meme and clip-view e2e coverage"
```

---

### Task 9: 로케일 페이지 + sitemap

**Files:**
- Create: `ko/tools/yt-meme.html`, `es/tools/yt-meme.html`, `zh/tools/yt-meme.html`
- Modify: `sitemap.xml`

clip-view는 언어 중립(텍스트는 파라미터에서 옴) + noindex → 로케일 복사본·sitemap 등록 없음.

- [ ] **Step 1: 로케일 페이지 생성**

`tools/yt-meme.html`을 각 로케일 디렉토리로 복사 후 **DOM id는 절대 변경하지 않고** 다음만 교체:
- `<html lang>`: `ko`/`es`/`zh-CN`
- canonical/og:url: `https://2convert.org/{ko|es|zh}/tools/yt-meme.html`
- title/description/h1/subtitle/라벨·버튼 문구 번역:

| 문자열 | ko | es | zh |
|---|---|---|---|
| title | 유튜브 클립 밈 메이커 - ConvertFile | Creador de Memes de Clips de YouTube - ConvertFile | YouTube片段表情包制作器 - ConvertFile |
| h1 | 😂 유튜브 클립 밈 메이커 | 😂 Creador de Memes de YouTube | 😂 YouTube片段表情包制作器 |
| Load | 불러오기 | Cargar | 加载 |
| Set start here | ⏱ 시작점 설정 | ⏱ Marcar inicio | ⏱ 设为起点 |
| Set end here | ⏱ 끝점 설정 | ⏱ Marcar fin | ⏱ 设为终点 |
| Preview loop | 🔁 구간 미리보기 | 🔁 Vista previa | 🔁 循环预览 |
| Top/Bottom text | 상단 텍스트/하단 텍스트 | Texto superior/inferior | 顶部文字/底部文字 |
| Copy Clip Link | 🔗 클립 링크 복사 | 🔗 Copiar enlace | 🔗 复制片段链接 |
| Copy embed code | 임베드 코드 복사 | Copiar código embed | 复制嵌入代码 |
| GIF summary | 🎞️ 진짜 GIF 파일이 필요하면 영상 파일을 업로드하세요 | 🎞️ ¿Necesitas un GIF real? Sube el archivo de vídeo | 🎞️ 需要真正的GIF文件？请上传视频文件 |
| Make GIF | 🎞️ GIF 만들기 | 🎞️ Crear GIF | 🎞️ 生成GIF |

에러 문구 등 JS 출력 문자열은 공유 JS라 영어 유지 (기존 도구들과 동일한 트레이드오프).

- [ ] **Step 2: sitemap 등록**

`sitemap.xml`의 meme-generator 블록(766행 부근) 패턴을 따라 yt-meme용 4개 `<url>` 블록(en/ko/es/zh, 각각 hreflang alternate 4종 + x-default) 추가.

- [ ] **Step 3: 검증**

- `http://127.0.0.1:8080/ko/tools/yt-meme.html` → 한국어 라벨, URL 로드→구간→링크 복사 동작 (공유 JS 정상)
- `xmllint --noout sitemap.xml` → 문법 오류 없음

- [ ] **Step 4: Commit**

```bash
git add ko/tools/yt-meme.html es/tools/yt-meme.html zh/tools/yt-meme.html sitemap.xml
git commit -m "feat: add yt-meme locale pages and sitemap entries"
```

---

## Self-Review 결과

- **Spec coverage:** URL 파싱(T1), 구간 선택/미리보기/에러코드(T4), 텍스트 바+정책 준수(T5), 공유 링크/embed/파라미터 검증(T5·T6), GIF 파이프라인/15초 한도/클램프 경고(T7), MemeText 추출(T2), sidebar/sitemap/로케일(T3·T9), 테스트(T1·T2·T8). 스펙의 "clip-view 로케일 복사본"은 언어 중립 페이지라 단일본으로 축소 — 스펙 취지(사이트 관례 준수)는 yt-meme 로케일 페이지로 충족.
- **Placeholder scan:** 통과 — 모든 코드 스텝에 실제 코드 포함.
- **Type consistency:** `getSegment()→{s,e}`, `displayText(raw)`, `YTUtils.*` 시그니처 태스크 간 일치 확인.
